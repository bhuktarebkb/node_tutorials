

// GLOBAL WINDOWS
/*
    __dirname -> returns the current directory path
    __filename - returns the current file name

    require() --> to import CommonJS
    CommonJS -> current module, every file is a module (by default)
    Modules = Encapsulated Code (obly share minimum)
    process -> which shows env
*/

console.log(__dirname);
console.log('--------------------------');

console.log(__filename);
console.log('--------------------------');

console.log(process.env);
